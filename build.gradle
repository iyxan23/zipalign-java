plugins {
    id 'java'
}

group 'com.iyxan23'
version '0.1-dev'

repositories {
    mavenCentral()
}

// zero dependencies :)
dependencies {}

// run `./gradlew jar` to generate a runnable jar
jar {
    manifest {
        attributes('Main-Class': 'com.iyxan23.zipalignjava.Main')
    }
}